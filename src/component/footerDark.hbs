<div class='footer'>
  {{#with footer}}
    <div class='nav_footer'>
      {{#each pages}}
        <a href='{{path}}' class='nav_link'>{{name}}</a>
      {{/each}}
    </div>
    <div><p class='footer_copyright'>{{copyright}}</p></div>
  {{/with}}
</div>